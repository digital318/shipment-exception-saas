import { CARRIER_DISPLAY_NAMES } from "./carrier-provider";
import { createMockProvider } from "./mock-responses";

export const fedexProvider = createMockProvider("fedex", CARRIER_DISPLAY_NAMES.fedex);
